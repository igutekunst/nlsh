# LLM API Configuration
# nlsh will prefer Anthropic if both keys are available

# Anthropic API Configuration (preferred)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Specify Anthropic model (default: claude-3-5-sonnet-20241022)
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# OpenAI API Configuration (fallback)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify OpenAI model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional: History database path (default: ~/.nlsh/history.db)
# NLSH_DB_PATH=/path/to/your/history.db 